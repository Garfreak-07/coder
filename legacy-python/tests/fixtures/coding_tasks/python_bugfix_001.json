{
  "task_id": "python_bugfix_001",
  "repo_fixture": "mini_python_project",
  "request": "Fix the failing validation test.",
  "check_commands": ["python -m unittest discover -s tests"],
  "expected_changed_files": ["src/users.py"],
  "forbidden_paths": [".env", ".git", ".coder"],
  "acceptance": {
    "tests_pass": true,
    "no_forbidden_files_changed": true
  }
}
