{
  "name": "coder-workbench",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "desktop:dev": "npx --yes @tauri-apps/cli@2 dev",
    "desktop:build": "npx --yes @tauri-apps/cli@2 build"
  }
}
